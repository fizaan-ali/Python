# LAMBDA FUNCTIONS -> Anonymous function without a name
# It has only one expression inside a body 

lambda num : num * 2   # first num is argument, second num * 2 is a single expression inside a lambda body

multiply = lambda num1, num2 : num1 * num2  # can't called directly have to assigned to some variables

print(multiply(4,6))